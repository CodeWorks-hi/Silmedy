# 🩺 Silmedy 환자용 안드로이드 앱

### 환자의 증상 입력, 화상 진료, 처방 확인까지 지원하는 모바일 클라이언트 앱

---

## 📌 앱 개요

Silmedy 환자용 앱은 비대면 진료 서비스를 이용하는 환자들을 위한 안드로이드 기반 모바일 클라이언트입니다. 사용자는 증상 입력, 화상 진료 참여, 진단 결과 및 처방 확인, 약 배송 요청 등의 기능을 간편하게 이용할 수 있습니다.

---

## 📱 주요 기능

### 📝 증상 입력 및 문진
- 증상 및 불편 사항을 텍스트 또는 선택형 방식으로 입력
- AI 기반 질환 분류를 통한 진단 전 참고 정보 제공
- TensorFlow Lite 모델을 활용한 온디바이스 AI 처리

### 📞 영상 진료 참여
- 의료진이 수락한 진료 요청에 대해 WebRTC 기반 영상 통화 자동 연결
- 화상 중 실시간 자막 제공 (의사 음성 인식 기반)
- Firebase RTDB를 통한 실시간 시그널링

### 📋 진단 및 처방 확인
- 진료 종료 후 진단서 및 처방 내용 확인
- 의약품 정보 및 복약 안내 확인 가능

### 🛍 약국 연계 및 배송 요청
- 카카오맵 기반 주변 약국 검색 및 선택
- 약 배송 요청 기능 제공
- 배송 상태 및 수령 여부 확인 가능

### 📈 진료 이력 열람
- 과거 진료 기록 및 처방 이력 조회
- 진료일, 증상, 진단 결과 등 확인 가능

---

## 🛠 기술 스택

| 분야            | 기술                                         |
|-----------------|--------------------------------------------|
| **언어**        | Java 11                                    |
| **프레임워크**  | Android SDK (minSdk 24, targetSdk 35)      |
| **빌드 시스템** | Gradle (Kotlin DSL)                        |
| **AI/ML**       | TensorFlow Lite 2.14.0, Hugging Face API   |
| **실시간 통신** | Firebase RTDB, WebRTC (125.6422.07)        |
| **네트워크**    | Retrofit 2.9.0, OkHttp 4.10.0              |
| **이미지 처리** | Glide 4.16.0, Glide Transformations        |
| **데이터베이스**| Firebase Firestore, Room 2.5.1             |
| **인증**        | Firebase Auth, JWT (Auth0)                 |
| **지도**        | Kakao Map API                              |
| **푸시 알림**   | Firebase Cloud Messaging (FCM)             |
| **위치 서비스** | Google Play Services Location              |
| **배포**        | Docker, Nginx                              |

---

## 📂 프로젝트 구조

```
📁 Silmedy/
├── 📂 app/                           # 메인 애플리케이션 모듈
│   ├── 📂 src/
│   │   ├── 📂 main/
│   │   │   ├── 📂 java/com/example/silmedy/
│   │   │   │   ├── 📂 ui/           # 화면 구성 (37개 액티비티/프래그먼트)
│   │   │   │   ├── 📂 adapter/      # RecyclerView 어댑터 모음
│   │   │   │   ├── 📂 model/        # 데이터 모델 클래스
│   │   │   │   ├── 📂 llama/        # AI 문진 처리 및 메시지 모델
│   │   │   │   ├── 📂 videocall/    # WebRTC 화상 진료 처리
│   │   │   │   ├── 📂 ai_model/     # Keras/TensorFlow 모델 예측
│   │   │   │   └── 📄 MainActivity.java
│   │   │   ├── 📂 assets/
│   │   │   │   ├── model_unquant.tflite  # AI 모델 파일
│   │   │   │   ├── map_kakao.html        # 카카오맵 WebView
│   │   │   │   └── sdk.js                # SDK 스크립트
│   │   │   └── 📂 res/              # 리소스 (레이아웃, 이미지 등)
│   │   ├── 📂 androidTest/          # 계측 테스트
│   │   └── 📂 test/                 # 단위 테스트
│   ├── 📄 build.gradle.kts          # 앱 빌드 설정
│   └── 📄 google-services.json      # Firebase 설정
├── 📂 apk/                          # 빌드된 APK 파일
├── 📂 data/                         # 테스트/더미 데이터
│   └── 📂 dummy_data/               # 샘플 CSV 및 이미지
├── 📂 gradle/                       # Gradle Wrapper
├── 📄 build.gradle.kts              # 루트 빌드 설정
├── 📄 settings.gradle.kts           # 프로젝트 설정
├── 📄 Dockerfile                    # 앱 빌드용 Docker 이미지
├── 📄 Dockerfile.nginx              # Nginx 배포용 Docker 이미지
├── 📄 nginx.conf                    # Nginx 설정
├── 📄 deploy-all.sh                 # 전체 배포 자동화 스크립트
└── 📄 README.md
```

---

## 🚀 설치 및 실행

### 📱 모바일 기기 (Android)

최신 APK 다운로드: **[Silmedy.apk](http://43.201.73.161:8080/Silmedy.apk)**

> ⚠️ **설치 안내**
> 
> 일부 Android 기기 및 PC 웹 브라우저에서는 `.apk` 파일이 바로 설치되지 않거나 차단될 수 있습니다.
> 
> **PC에서 다운로드한 경우:** `.apk` 파일을 Android 기기로 전송한 후 설치하거나, Android 기기에서 직접 위 링크를 여는 것을 권장합니다.
> 
> **설치 절차:**
> 1. 위 링크를 클릭하여 `.apk` 파일을 다운로드합니다.
> 2. 다운로드가 완료되면 알림창 또는 파일 앱을 통해 `.apk` 파일을 실행합니다.
> 3. 설치가 차단되는 경우, 설정에서 **"알 수 없는 앱 설치 허용"**을 활성화합니다.
> 4. 설치가 완료되면 앱을 실행할 수 있습니다.

### 💻 개발 환경 (Android Studio)

#### 사전 요구사항
- Android Studio Hedgehog 이상
- JDK 11 이상
- Android SDK (API 24-35)
- Firebase 프로젝트 설정

#### 설정 단계

1. **프로젝트 클론 및 열기**
```bash
git clone <repository-url>
cd Silmedy
```

2. **환경 변수 설정**

`local.properties` 파일에 다음 키 추가:
```properties
KAKAO_NATIVE_APP_KEY=your_kakao_key
HUGGINGFACE_API_KEY=your_huggingface_key
HUGGINGFACE_API_URL=your_huggingface_url
```

3. **Firebase 설정**
- Firebase Console에서 프로젝트 생성
- `google-services.json` 파일을 `app/` 디렉터리에 추가
- Firebase Authentication, Firestore, RTDB, Storage, FCM 활성화

4. **빌드 및 실행**
```bash
./gradlew assembleDebug
# 또는 Android Studio에서 Run 버튼 클릭
```

---

## 🐳 Docker 기반 배포

### 자동 배포 스크립트

전체 빌드 및 배포를 자동화하는 스크립트:

```bash
./deploy-all.sh
```

#### 배포 프로세스
1. **Gradle 빌드**: Release APK 생성
2. **APK 복사**: `apk/Silmedy.apk`로 복사
3. **Docker 이미지 빌드**: Nginx 기반 정적 파일 서버
4. **이미지 압축**: `silmedy-distribution.tar.gz` 생성
5. **EC2 전송**: SCP를 통한 이미지 전송
6. **컨테이너 실행**: EC2에서 Docker 컨테이너 자동 시작

### 수동 배포

#### APK 빌드
```bash
./gradlew clean assembleRelease
cp app/build/outputs/apk/release/app-release-unsigned.apk apk/Silmedy.apk
```

#### Docker 이미지 빌드
```bash
docker build --platform=linux/amd64 -f Dockerfile.nginx -t silmedy-distribution .
```

#### 컨테이너 실행
```bash
docker run -d --name silmedy-nginx -p 8080:80 silmedy-distribution
```

---

## 🧩 개발 노트

### 인증 및 보안
- **AccessToken 자동 갱신**: `TokenManager` 클래스를 통한 JWT 토큰 관리
- **JWT 디코딩**: Auth0 라이브러리 활용
- **네트워크 보안**: `network_security_config.xml`을 통한 보안 설정

### AI/ML 처리
- **TensorFlow Lite**: 온디바이스 모델 추론 (`model_unquant.tflite`)
- **Hugging Face API**: 외부 AI 모델 연동
- **응답 시간**: Llama 응답은 최대 2초 내 반환, UI 반영은 비동기 처리

### 실시간 통신
- **WebRTC**: P2P 영상 통화 구현
- **Firebase RTDB**: 시그널링 및 상태 동기화
- **FCM**: 백그라운드 푸시 알림 및 WebRTC 종료 신호

### 성능 최적화
- **이미지 로딩**: Glide 캐싱 및 변환 적용
- **ProGuard**: Release 빌드 최적화 (필요 시 활성화)
- **Room**: 로컬 데이터베이스 캐싱

---

## 🧪 테스트

### 단위 테스트
```bash
./gradlew test
```

### 계측 테스트
```bash
./gradlew connectedAndroidTest
```

---

## 📄 라이선스

이 프로젝트는 비공개 프로젝트입니다.

---

## 👥 팀

**CodeWorks Team**

---

## 📞 문의

프로젝트 관련 문의사항은 팀 리포지토리를 통해 연락해주세요.
