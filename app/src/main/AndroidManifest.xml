<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.silmedy">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Silmedy"
        tools:targetApi="31">
        <activity
            android:name=".ClinicHomeActivity"
            android:exported="false" />
        <activity
            android:name=".HandActivity"
            android:exported="false" /> <!-- BobyMain 액티비티: 패키지 경로 정확하게 지정 -->
        <activity
            android:name=".BobyMain"
            android:exported="true" /> <!-- MainActivity: 앱 런처 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>